# 服务配置
PROJECT_NAME=向量数据库MCP服务
PROJECT_DESCRIPTION=基于Qdrant的向量数据库MCP服务，使用BERT模型进行向量化
PROJECT_VERSION=0.1.0
DEBUG=true
HOST=0.0.0.0
PORT=8000
API_PREFIX=/api/v1

# 向量数据库配置 (Qdrant)
VECTOR_DB_HOST=localhost
VECTOR_DB_PORT=6333
VECTOR_DB_GRPC_PORT=6334
VECTOR_DB_API_KEY=
VECTOR_DB_HTTPS=false

# 嵌入模型配置
EMBEDDING_PROVIDER=sentence-transformers
# 中文BERT模型推荐: BAAI/bge-small-zh, BAAI/bge-large-zh, shibing624/text2vec-base-chinese
# 英文BERT模型推荐: paraphrase-MiniLM-L6-v2, all-MiniLM-L6-v2, all-mpnet-base-v2
EMBEDDING_MODEL=BAAI/bge-small-zh
EMBEDDING_DEVICE=cpu
# 如果有GPU，可以设置为cuda
# EMBEDDING_DEVICE=cuda

# MCP服务器配置
MCP_SERVER_ENABLED=true
MCP_SERVER_PATH=/mcp
MCP_SERVER_NAME=向量数据库MCP服务
MCP_SERVER_DESCRIPTION=提供向量存储和检索功能的MCP服务